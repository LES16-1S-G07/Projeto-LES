---
declared-services:
  mean-mongo:
    label: mongodb
    plan: 100
applications:
- name: fatec-gym
  host: fatec-gym-${random-word}
  memory: 128M
  services:
  - mean-mongo
  env:
    NODE_ENV: cloud-foundry
